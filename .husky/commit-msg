#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

StartSTR='->  PRE-COMMIT HOOK STARTING...\n'
FinishSTR='->  PRE-COMMIT HOOK CHECKS FINISHED\n'

echo "$StartSTR"
npx --no -- commitlint --edit
echo "$FinishSTR"
